1. Need to install all essenitial packages `npm install` in the directory of the project.
2. Should at first run 'npm start'
3. As next run 'npm run android' which should run build of the app on mobile device eg.
